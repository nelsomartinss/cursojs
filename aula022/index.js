// Operação ternária ? : 

// if = ? 
// else = :

// Exemplo pratico 
const nota = 10; 
const resultado = nota <= 6 ? 'Reprovado' : 'Aprovado'; // verificando nota
console.log(resultado); // exibindo 