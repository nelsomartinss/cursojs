console.log('Podemos executar os códigos com node nomedoarquivo.js')

// usaremos o terminal para usar o comando node index.js para executar esse arquivo
// usamos cd nomedapasta para acessar a pasta que estamos utilizando
// usamos clear ou cls para limpar o terminal (dependendo do terminal)

console.log('esse texto será exibindo no console do navegador.')

// é interessate sempre usar um arquivo a parte para usar o javascript

alert('Esse é um comando que só vai funcionar no navegador, no node não vai funcionar!'); // o comando alert exibe uma caixinha no navegador que vai travar o carregamento do site até o usuário clicar em 'enter'

// se tentarmos executar o alert() no node ele vai resultar em alert is not defined, no navegador vai funcionar