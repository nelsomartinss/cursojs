// entrada
const primeiroNumero = prompt('Digite um número:');
const segundoNumero = prompt('Digite outro número:');

// processamento
const resultadoDaAdicao = Number(primeiroNumero) + Number(segundoNumero); // conversao da string para numero

// se o valor não vai mudar, use const, se pretende mudar, use let, avalie isso sempre que for criar uma variavel

// saida
alert(`O resultado da sua conta foi: ${resultadoDaAdicao}`); 